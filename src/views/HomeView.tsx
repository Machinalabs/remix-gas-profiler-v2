import React from "react"

// Example how to use context

// import { AppContext } from "../AppContext"
// const { protocols } = useContext(AppContext)

// const [hasError, setHasError] = useState(false)

export const HomeView: React.FC = () => {
  return (
    <div>
      <h1>Home</h1>
    </div>
  )
}